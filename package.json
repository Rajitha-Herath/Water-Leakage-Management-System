{
  "name": "nwsdb-water-leakage-management-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install && npm --prefix mobile-expo install",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:mobile": "npm --prefix mobile-expo start",
    "check:mobile": "npm --prefix mobile-expo run typecheck",
    "test": "npm --prefix server test && npm --prefix web test",
    "build": "npm --prefix web run build"
  }
}
